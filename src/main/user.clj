; modification of https://github.com/olsh/datagrip-poco-generator/blob/master/deprecated/Generate%20POCO.clj
(ns user
  (:require [clojure.core :refer :all])
  )
(println ROWS)
(.append OUT "hello")
; used in the script dire and the things works